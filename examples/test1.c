void empty() { }

void simple_arith_with_arg(int d) {
  (d > d/2) || (d >= 100) && (d < 99);
}

int printf(char const *format, ...);

void simple_arith() {
  int x;
  nowhere:
  x = (10 - 10/3) << 3 | (23+8*12) & 1024;
  x = 10.23812;
  empty();
}